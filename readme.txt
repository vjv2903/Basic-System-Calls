HOW TO RUN BOTH THE FILES

Part 1:

To compile the code 		: gcc Part1.c -lm
To run the code with the file 	: ./a.out input_filename.txt 
The code handles most of the possible error causing cases.
The code creates the directory Assignment directory and save the reversed file in that directory.


Part 2:

To compile the code 		: gcc Part2.c -lm
To run the code with the files 	: ./a.out newfile oldfile directory
The code handles most of the possible error causing cases.

